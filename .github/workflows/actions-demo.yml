on: [push]

jobs:
  build:
    name: Greeting
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a multi-line script
      run: |
        choco install ffmpeg
        echo Running Application to record video
        chmod +x Recorder.exe
        ./Recorder.exe
